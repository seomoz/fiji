End-to-end test for the FijiMusic tutorial.

To run the test:
  Set the python path to the root directory:
    export PYTHONPATH=$PYTHONPATH:$PWD/..

  On a released version:
    ./fiji_music.py \
        --log_level=DEBUG \
        --fiji_bento_version=1.0.0-rc5

  On a FijiBento version you built locally on your machine:
    ./fiji_music.py \
        --log_level=5 \
        --fiji_bento_version=1.0.0-rc6-SNAPSHOT \
        --maven_local_repo=$HOME/.m2/repository

Requirements: Python3, Maven3, Java6.

Notes:
 - BentoCluster won't start automatically if the "defaults" Hadoop ports are not available.
   (See https://jira.fiji.org/browse/CLUSTER-21)
